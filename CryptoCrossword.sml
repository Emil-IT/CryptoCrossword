(* CryptoCrossword *)

(*
REPRESENTATION CONVENTION: 
REPRESENTATION INVARIANT:
*)
datatype CrossWord = CW of int list list

(*
solve x
TYPE:
PRE:
POST:
EXAMPLE:
*)
fun solve crossword = 
let
	
in
end